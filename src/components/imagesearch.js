import React from "react";

const SearchInput = props =>
(
  <form id = "testg" onSubmit={props.changeDate1 }align="center" >
  <h6>  Search an image:</h6>
    <input />
    <input type="submit" />
  </form>
);

export default SearchInput;
